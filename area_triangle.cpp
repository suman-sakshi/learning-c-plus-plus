#include<iostream>
#include<cmath>
using namespace std;
int main()
{
    float radius,area;
    cout<<"enter the valuse of radius: ";
    cin>>radius;
    area= 3.14*pow(radius,2);
    cout<<"the area is "<<area<<endl;
    return 0;
}